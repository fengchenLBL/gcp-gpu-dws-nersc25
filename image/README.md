# Packer setup

Packer install info is here: [https://developer.hashicorp.com/packer/install](https://developer.hashicorp.com/packer/install)

Before building the image run:

`packer plugins install github.com/hashicorp/googlecompute`
